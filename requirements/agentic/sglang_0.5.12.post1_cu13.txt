# engine: sglang==0.5.12.post1
# sglang 0.5.12.post1 on CUDA 13 (torch 2.11 cu130, driver >= 580).
-r sglang_0.5.12.post1_common.txt

cuda-python>=13.0
flash-attn-4>=4.0.0b9
# flashinfer: sglang pins 0.6.11.post1 exactly and vLLM 0.23 pins 0.6.12. Both
# work with sgl_kernel (verified on the rmsnorm path), so use vLLM's, which is
# newer -- in a shared venv the two engines have to agree, otherwise whichever
# is installed second silently downgrades the other's kernels.
flashinfer-cubin==0.6.12
flashinfer-python==0.6.12
# sglang declares 4.5.1, but in that release the generated MLIR dialect calls an
# OpView constructor its own native bindings do not have, so every cute.compile
# dies with "GPUModuleOp.__init__(): incompatible function arguments" -- which is
# the path flashinfer's rmsnorm takes when there is no prebuilt cubin for the
# GPU (e.g. sm80). 4.5.2 fixes it, and is also what vLLM 0.23 requires, so the
# two engines agree on the shared library whichever order they are installed in.
nvidia-cutlass-dsl[cu13]==4.5.2
quack-kernels>=0.4.1
sgl-deep-gemm==0.1.0
sglang-kernel==0.4.2.post2
tilelang==0.1.8
